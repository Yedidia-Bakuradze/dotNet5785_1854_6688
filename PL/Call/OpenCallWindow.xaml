<Window x:Class="PL.Call.OpenCallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        mc:Ignorable="d"
        Title="OpenCallWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="{Binding CurrentCall.Id}" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="{Binding CurrentCall.TypeOfCall}" FontSize="16" Margin="5,10,0,0" />
            <TextBlock Text="{Binding CurrentCall.Description}" FontSize="14" Margin="5,10,0,0" TextWrapping="Wrap" />
        </StackPanel>
        <Grid Grid.Column="1">
            <!-- Future features will be added here -->
        </Grid>
    </Grid>
</Window>
